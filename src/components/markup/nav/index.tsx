import * as React from 'react'
import styled, { styledEl } from '../../../styles/styled'
import { HAlignFlex, HBox } from '../../layout/Box'

export const Nav = styledEl(styled.nav)`
  ${HAlignFlex}
`
